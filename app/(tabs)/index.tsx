// Screen 1 — Planning for weeks or months ahead (SRS §4.2).

import { PlanningWizard } from '../../src/ui/wizard/PlanningWizard';

export default function PlanScreen() {
  return <PlanningWizard />;
}
